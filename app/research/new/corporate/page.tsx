import CorporateProfileForm from "@/components/corporate-profile-form";

export const metadata = {
  title: "Corporate Profile Builder | Catapult Fundraising",
  robots: { index: false, follow: false },
};

export default function ResearchNewCorporatePage() {
  return <CorporateProfileForm />;
}
